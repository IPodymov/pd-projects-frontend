# Инструкция по установке и запуску

## 📋 Требования

- Node.js 16+ 
- npm или yarn
- Работающий backend на http://localhost:3000 (development)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

Это установит:
- `vue@^3.5.24` - фреймворк
- `vue-router@^4.4.5` - система маршрутизации

### 2. Конфигурация окружения

Файлы уже созданы с правильными значениями:

**`.env`** (для development):
```
VITE_API_URL_DEV=http://localhost:3000
VITE_API_URL_PROD=https://api.pd-projects.com
```

**`.env.production`** (для production):
```
VITE_API_URL_PROD=https://api.pd-projects.com
```

> При необходимости измените URL в зависимости от вашего backend.

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Откройте браузер на http://localhost:5173

### 4. Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`.

### 5. Просмотр production сборки

```bash
npm run preview
```

## 🔧 Структура проекта

```
src/
├── components/
│   ├── Login.vue                  # ✅ Готов (форма входа)
│   ├── Register.vue               # ✅ Готов (форма регистрации)
│   └── HelloWorld.vue             # (пример компонента)
├── config/
│   └── config.js                  # ✅ Готов (конфигурация)
├── services/
│   └── api.js                     # ✅ Готов (API сервис с DRY)
├── store/
│   └── store.js                   # ✅ Готов (управление состоянием)
├── router/
│   └── router.js                  # ✅ Готов (навигация)
├── assets/
│   ├── fonts/                     # ✅ Подключены все шрифты Onest
│   ├── icons/
│   ├── images/
│   └── main.css
├── App.vue                        # ✅ Обновлен с навигацией
├── main.js                        # ✅ Обновлен с роутером
├── style.css                      # ✅ Палитра цветов + шрифты
└── vite.config.js
```

## 🎨 Что уже реализовано

✅ **Аутентификация и регистрация**
- Форма входа (Login.vue)
- Форма регистрации (Register.vue)
- API сервис с DRY принципами

✅ **Управление состоянием**
- Реактивное хранилище (store.js)
- Сохранение в localStorage
- Автоматическое восстановление при перезагрузке

✅ **Конфигурация окружений**
- Разные URL для dev и prod
- Автоматическое переключение

✅ **Навигация**
- Vue Router 4
- Защита маршрутов (requiresGuest)

✅ **Дизайн**
- Цветовая палитра «Минимализм и ясность»
- Шрифт Onest (все 9 вариантов)
- Красивые стили для форм

## 📡 Тестирование API

### 1. Убедитесь, что backend работает

```bash
# В отдельном терминале
cd path/to/pd-projects-backend
npm run dev
```

Backend должен работать на http://localhost:3000

### 2. Запустите frontend

```bash
npm run dev
```

### 3. Тестируйте в браузере

1. Откройте http://localhost:5173
2. Вас перенаправит на `/login`
3. Нажмите на "Зарегистрируйтесь"
4. Заполните форму и отправьте
5. Вы получите JWT токен и будете авторизованы

## 🐛 Решение проблем

### Ошибка CORS при запросе к API

**Решение**: Убедитесь, что backend включил CORS для http://localhost:5173

```javascript
// В backend (main.ts или app.js)
app.use(cors({
  origin: ['http://localhost:5173', 'http://localhost:3000'],
  credentials: true
}));
```

### Токен не сохраняется

**Решение**: Проверьте, что localStorage не отключен в браузере

### 401 Unauthorized при запросах

**Решение**: Это может быть:
1. Токен истек (автоматически удалится и перенаправит на логин)
2. Backend вернул неверный formato token
3. Пароль неверный

## 🔐 Безопасность

- JWT токены хранятся в localStorage
- Все запросы содержат Authorization header
- Токен удаляется при 401 ошибке
- Пароли не отправляются повторно
- HTTPS требуется в production

## 📝 API Endpoints (из backend)

### Аутентификация
- `POST /auth/register` - регистрация
- `POST /auth/login` - вход
- `POST /auth/invitation` - приглашение

### Пользователи
- `GET /users` - список
- `GET /users/search?email=...` - поиск
- `PATCH /users/:id` - обновление
- `POST /users/github/link` - GitHub

### Проекты
- `GET /projects` - список
- `POST /projects` - создание
- `GET /projects/:id` - получить
- `PATCH /projects/:id` - обновить
- `PATCH /projects/:id/status` - статус
- `POST /projects/:id/join` - присоединиться
- `POST /projects/:id/upload` - загрузка файла
- `DELETE /projects/:id` - удалить

## 📚 Документация

- [AUTHENTICATION.md](./AUTHENTICATION.md) - Подробная документация по системе аутентификации
- [EXAMPLES.js](./EXAMPLES.js) - Примеры использования API

## 🎯 Следующие шаги

После установки вы можете:

1. **Добавить больше компонентов**
   ```bash
   # Например, Dashboard.vue, ProjectList.vue и т.д.
   ```

2. **Использовать API сервисы**
   ```javascript
   import { projectService } from '@/services/api.js';
   
   const projects = await projectService.getProjects();
   ```

3. **Интегрировать компоненты в маршруты**
   ```javascript
   // router.js
   {
     path: '/projects',
     component: ProjectList,
     meta: { requiresAuth: true }
   }
   ```

4. **Добавить состояние для других данных**
   ```javascript
   // Расширить store.js новыми actions и mutations
   ```

## 💡 Полезные команды

```bash
# Установить зависимости
npm install

# Запуск development сервера
npm run dev

# Сборка для production
npm run build

# Просмотр production сборки
npm run preview
```

## ✉️ Поддержка

Если у вас есть вопросы, обратитесь к:
- Backend репозиторий: https://github.com/IPodymov/pd-projects-backend
- Документация Vue 3: https://vuejs.org
- Документация Vue Router: https://router.vuejs.org

---

**Готово к разработке!** 🚀
