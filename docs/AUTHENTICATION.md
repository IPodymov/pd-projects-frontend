# Документация по системе аутентификации

## Обзор

Система реализована с использованием методологии DRY (Don't Repeat Yourself) для минимизации дублирования кода.

## 📁 Структура проекта

```
src/
├── components/
│   ├── Login.vue              # Форма входа
│   └── Register.vue           # Форма регистрации
├── config/
│   └── config.js              # Конфигурация приложения (URLs, endpoints)
├── services/
│   └── api.js                 # Сервисы API (DRY принципы)
├── store/
│   └── store.js               # Управление состоянием (состояние пользователя)
├── router/
│   └── router.js              # Навигация приложения
├── App.vue                    # Главный компонент
├── main.js                    # Точка входа
├── style.css                  # Глобальные стили и цветовая палитра
└── .env                       # Переменные окружения
```

## 🔑 Ключевые компоненты

### 1. **config/config.js** - Конфигурация окружений
- Автоматически переключается между dev и prod URL
- Определяет все API endpoints
- Использует переменные окружения VITE_API_URL_DEV и VITE_API_URL_PROD

```javascript
// Development (по умолчанию)
apiBaseUrl: 'http://localhost:3000'

// Production
apiBaseUrl: 'https://api.pd-projects.com'
```

### 2. **services/api.js** - API сервис (DRY)
Единая функция `apiCall()` для всех запросов:
- Автоматическое управление токенами
- Обработка ошибок
- Работа с JSON
- Проверка статусов

**Сервисы:**
- `authService.register()` - регистрация
- `authService.login()` - вход
- `authService.logout()` - выход
- `userService.*` - операции с профилем
- `projectService.*` - операции с проектами

### 3. **store/store.js** - Управление состоянием
Реактивное хранилище с использованием Composition API:
- `state` - реактивное состояние пользователя
- `mutations` - методы изменения состояния
- `actions` - асинхронные операции
- `useAuth()` - composable для использования в компонентах

Автоматически сохраняет/восстанавливает данные из localStorage.

### 4. **components/Login.vue** и **Register.vue**
- Красивые формы с валидацией
- Использование Composition API
- Применение цветовой палитры
- Обработка ошибок

### 5. **router/router.js** - Навигация
- Защита маршрутов (гостевые маршруты)
- Автоматическое перенаправление

## 🚀 Использование

### Вход в систему
```javascript
import { useAuth } from '@/store/store.js';

const { login, isLoading, error } = useAuth();

// Вход
await login({ email: 'user@example.com', password: 'password' });
```

### Регистрация
```javascript
const { register } = useAuth();

await register({
  name: 'Иван Петров',
  email: 'ivan@example.com',
  password: 'password',
  schoolId: '1', // опционально
  token: 'invitation-token' // опционально
});
```

### Проверка авторизации
```javascript
const { isAuthenticated, user } = useAuth();

if (isAuthenticated.value) {
  console.log(user.value.name);
}
```

### Работа с API
```javascript
import { projectService } from '@/services/api.js';

// Получить проекты
const projects = await projectService.getProjects();

// Создать проект
await projectService.createProject({
  title: 'Мой проект',
  description: 'Описание',
  schoolId: '1'
});
```

## 🎨 Цветовая палитра

Палитра «Минимализм и ясность»:
- **Primary**: `#005BBB` (глубокий синий)
- **Secondary**: `#E1E8ED` (светло-серый)
- **Accent**: `#FF6F00` (оранжевый)
- **Background**: `#FFFFFF`
- **Text**: `#2C2C2C`

Используется через CSS переменные:
```css
background-color: var(--color-primary);
color: var(--color-text);
border: 1px solid var(--color-border);
```

## 🔒 Безопасность

1. **Токены JWT** хранятся в localStorage
2. **Автоматическое удаление токена** при 401 ошибке
3. **Все запросы** содержат Authorization header
4. **Парольная защита** с подтверждением

## 📦 Переменные окружения

### .env (Development)
```
VITE_API_URL_DEV=http://localhost:3000
VITE_API_URL_PROD=https://api.pd-projects.com
```

### .env.production
```
VITE_API_URL_PROD=https://api.pd-projects.com
```

## 🧪 Тестирование API

1. **Запустить бекенд** на http://localhost:3000
2. **Запустить фронтенд** командой `npm run dev`
3. **Открыть** http://localhost:5173
4. **Регистрация/Вход** через формы

## 📝 Примечания

- Шрифт **Onest** подключен в style.css (все 9 вариантов начертания)
- Система использует **Composition API** Vue 3
- Состояние сохраняется в localStorage и восстанавливается при перезагрузке
- При выходе пользователя все данные очищаются
- API ошибки показываются пользователю в красивом формате

## 🔄 API Endpoints (из бека)

### Аутентификация
- `POST /auth/register` - регистрация
- `POST /auth/login` - вход
- `POST /auth/invitation` - создание приглашения

### Пользователи
- `GET /users` - список пользователей
- `GET /users/search?email=...` - поиск по email
- `PATCH /users/:id` - обновление профиля
- `POST /users/github/link` - связь с GitHub

### Проекты
- `GET /projects` - список проектов
- `POST /projects` - создание проекта
- `GET /projects/:id` - получить проект
- `PATCH /projects/:id` - обновить проект
- `PATCH /projects/:id/status` - изменить статус
- `POST /projects/:id/join` - присоединиться
- `POST /projects/:id/upload` - загрузить файл
- `DELETE /projects/:id` - удалить проект

## ⚙️ Установка зависимостей

```bash
npm install
```

Будут установлены:
- `vue@^3.5.24`
- `vue-router@^4.4.5`

## 🏃 Запуск

### Development
```bash
npm run dev
```

### Production Build
```bash
npm run build
npm run preview
```

---

Система полностью готова к использованию! 🎉
